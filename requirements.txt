# ─────────────────────────────────────────────────────────────
# requirements.txt — Finance RAG + CrewAI
# Target: Python 3.12.x | Linux (Render) & Windows local
#
# IMPORTANT: Pin Python to 3.12 on Render by adding a
# .python-version file to your repo root containing: 3.12.7
#
# Python 3.14 is NOT supported — chromadb has no working
# wheels for 3.14 until 1.5.2+, which breaks other deps.
# Python 3.12 has pre-built wheels for everything below.
# ─────────────────────────────────────────────────────────────

# ── Vector DB ─────────────────────────────────────────────────
# 0.5.23: last pure-Python backend, no Rust crash on any OS
chromadb==0.5.23

# ── Embeddings ────────────────────────────────────────────────
# 2.7.0: last release with tokenizers<=0.20 (matches chromadb)
sentence-transformers==2.7.0

# ── LangChain ─────────────────────────────────────────────────
langchain==0.2.16
langchain-community==0.2.16
langchain-text-splitters==0.2.4

# ── Document Loaders ─────────────────────────────────────────
pypdf==4.3.1
python-docx==1.1.2
unstructured==0.15.0

# ── Hybrid Search ─────────────────────────────────────────────
rank_bm25==0.2.2

# ── Gemini API ────────────────────────────────────────────────
google-generativeai==0.7.2

# ── CrewAI ────────────────────────────────────────────────────
# Install core only — crewai-tools conflicts via embedchain
# After pip install -r requirements.txt, run:
#   pip install crewai==0.86.0 --no-deps
#   pip install litellm opentelemetry-api opentelemetry-sdk opentelemetry-exporter-otlp-proto-http json_repair

# ── MongoDB ───────────────────────────────────────────────────
pymongo==4.8.0

# ── Web Server ────────────────────────────────────────────────
fastapi==0.111.0
starlette==0.37.2
uvicorn[standard]==0.30.6

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
rich==13.7.1
pydantic==2.8.2
